# 依赖
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# 构建产物
dist/
build/
.vite/
*.tsbuildinfo

# 环境变量
.env
.env.local
.env.*.local

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# 测试与覆盖率
coverage/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 运行时数据
*.log
.dev-logs/
# 只忽略仓库根的 data/（docker compose 的 postgres 绑定目录）。
# 不能写成 data/：那会连共享配置目录 shared/data/ 一起忽略，
# 导致新增的 shared/data/*.json 静默不入库、镜像构建缺文件。
/data/
*.db
*.sqlite
*.sqlite3
