# - Prefers Simplified Chinese (简体中文) for planning documents, clarifying questions, and all communication with them. Confidence: 0.9
- Prefers Simplified Chinese (简体中文) for planning documents, clarifying questions, and all communication with them. Confidence: 0.9
- Prefers Vue 3 + Vite as the frontend stack. Confidence: 0.7
- Cares about front-end page performance when many elements render at once: keeps UI assets (e.g. item/equipment icons) as small as possible so list-heavy pages don't lag. Confidence: 0.75
- Accepts pragmatic reuse in generated content/assets (e.g. the same base design recolored/redecorated) instead of every item getting a unique bespoke asset. Confidence: 0.6
- Prefers fine-grained time displays in the UI (e.g. 0.1s precision for cooldowns/countdowns) so values visibly tick and convey a sense of dynamic change. Confidence: 0.6
- Prefers fine-grained time displays in the UI (e.g. 0.1s precision for cooldowns/countdowns) so values visibly tick and convey a sense of dynamic change. Confidence: 0.6
- Wants all user-facing copy to be Chinese: no raw English identifiers leaking into the UI (attribute ids like `physDef`/`crit`/`dh`, effect/trigger enums, rarity or category slugs). Data should carry Chinese display names and views must map ids to them. Confidence: 0.85
- For game balance, prefers level gating over gear power: a player at a given level should only be able to beat monsters of a matching level, and even generously geared players must not trivially one-shot much higher-level monsters. Confidence: 0.7
- Wants in-game prices/fees to be always visible on the page (e.g. hero recruit cost shown on the button and in the confirm dialog), and dislikes actions that silently deduct currency without the player seeing the amount. Confidence: 0.6
- Insists displayed values match what the server actually enforces (e.g. recruitment cost shown as 50k while the server requires 64500), favoring one authoritative source of truth so the client and server can't diverge. Confidence: 0.6
- Prefers item/equipment lists to be sortable by power (战力) descending with the highest-power entries on top, exposed via a sort control (dropdown/one-click) in list-heavy views like the backpack and the equipment picker. Confidence: 0.6
- Cares about game-economy integrity: one-time/limited rewards must be idempotent and not re-grantable, so replaying a feature (e.g. re-watching the tutorial) cannot be exploited to farm currency/items. Confidence: 0.6
- Prefers graceful degradation over error/blank states: when the player has no hero, the game should render a sensible default (e.g. a default adventurer) instead of failing the whole state load and blanking the UI. Confidence: 0.55
- Wants the economy protected against infinite currency loops: no repeatable action may have positive expected value (e.g. buying a chest and selling what it yields), so costs/sell prices must leave the long-run expectation net-negative and only rare lucky outcomes (e.g. low-probability high-rarity drops) may turn a profit. Confidence: 0.65
