# 艾欧泽亚放置录（Eorzea Idle Chronicle）

挂机/放置类网页游戏。英雄在后台自动打怪，用掉落金币开箱抽取装备，装备带随机属性与 Buff/Debuff，
通过穿戴、合成、重造、附魔不断变强，逐关推进 40 个地区关卡。

需求来源：本仓库根目录的 [`prd.md`](./prd.md)（全量实现）。
实施计划：`~/.commandcode/plans/ff14-idle-chronicle-plan.md`。

> 本项目为同人练习项目，与《最终幻想 XIV》官方无关。

---

## 技术栈

| 层 | 技术 |
| --- | --- |
| 前端 | Vue 3 + Vite 6 + TypeScript + Pinia + Vue Router + Tailwind CSS 4 |
| 后端 | Python 3.11+ / FastAPI + SQLAlchemy 2.0 (async) + Alembic |
| 数据库 | 本地开发 SQLite；服务器部署 PostgreSQL 16 |
| 部署 | 本地一键脚本 `scripts/dev.sh`；服务器 `docker compose`（nginx + API + PG） |
| 共享层 | `shared/` 单一事实来源：配置 JSON + 双端加载器 |

---

## 目录结构

```
FF14Push/
├── prd.md                      需求文档
├── docker-compose.yml          服务器部署编排（db + backend + frontend）
├── .env.example                部署环境变量模板
├── .env.local.example          本地开发环境变量模板
├── .npmrc                      npm 国内镜像源（npmmirror）
├── scripts/dev.sh              ★ 本地一键启动脚本（无需 Docker）
├── shared/                     ★ 前后端共享配置层（唯一事实来源）
│   ├── data/*.json           品阶/栏位/职业/底材/词条/怪物/BOSS/地区/副本/箱子/合成/经济/资质/指引
│   └── schema/
│       ├── index.ts          TS 类型 + 底材展开 + 命名导出
│       └── loader.py         Python 加载器（与 index.ts 同源）
├── backend/                  FastAPI 服务
│   ├── Dockerfile              后端镜像
│   ├── docker-entrypoint.sh    等库 → 迁移 → 启动
│   ├── alembic/                数据库迁移
│   ├── app/
│   │   ├── api/v1/             路由：auth / game / battle / inventory / chest /
│   │   │                       economy / region / tavern / codex / ranking / tutorial / settings /
│   │   │                       friends / coop / arena / worldboss
│   │   ├── core/               配置、DB、JWT、依赖
│   │   ├── models/             ORM 模型
│   │   ├── services/           数值引擎、掉落、战斗模型、校验、图鉴、排行…
│   │   └── main.py             应用入口（建表开关 + 排行榜定时刷新）
│   └── tests/                  94 项 pytest（共享数据一致性 / 数值引擎 / 接口集成）
└── frontend/                   Vue3 前端
    ├── Dockerfile              前端镜像（构建 → nginx）
    ├── nginx.conf              静态站点 + /api 反向代理
    └── src/
        ├── api/                axios 封装
        ├── stores/             Pinia：auth / game / toast / itemActions
        ├── game/core/          战斗模拟、伤害、怪物属性、类型定义
        ├── components/         通用组件（物品卡、弹窗、指引、日志）
        └── views/              13 个页面
```

---

## 国内网络环境（依赖源）

默认全部走国内镜像，开箱即用于国内服务器：

| 用途 | 默认源 | 覆盖变量 |
| --- | --- | --- |
| Docker 基础镜像 | `docker.m.daocloud.io/library/` | `DOCKER_IMAGE_MIRROR`（`.env`） |
| pip（后端依赖） | `pypi.tuna.tsinghua.edu.cn` | `PIP_INDEX_URL`（`.env` / `.env.local`） |
| npm（前端依赖） | `registry.npmmirror.com` | `NPM_REGISTRY`（`.env` / `.env.local`）或根目录 `.npmrc` |
| Debian apt（镜像内装 curl） | `mirrors.tuna.tsinghua.edu.cn` | `APT_MIRROR`（`.env`） |

- **Docker 部署**：`docker compose up -d --build` 会用上述源拉基础镜像并安装依赖。
  把对应变量在 `.env` 中设为**空值**即回退官方源——例如服务器已在
  `/etc/docker/daemon.json` 配好 `registry-mirrors` 时，留 `DOCKER_IMAGE_MIRROR=` 空着即可，
  镜像名会回到 `postgres:16-alpine` 这种官方写法。
- **本地开发**：`./scripts/dev.sh` 安装依赖时默认走清华 pip 与 npmmirror；
  直接 `npm install` 则由仓库根目录的 `.npmrc` 生效。
- 绕过 compose 直接 `docker build` 时，用 `--build-arg` 传同名参数
  （`PYTHON_IMAGE` / `NODE_IMAGE` / `NGINX_IMAGE` / `PIP_INDEX_URL` / `NPM_REGISTRY` / `APT_MIRROR`）。

---

## 本地开发（无 Docker）

```bash
./scripts/dev.sh
```

首次运行会自动：生成 `.env.local` → 创建 Python 虚拟环境 → 安装前后端依赖 →
用 **SQLite** 启动后端（自动建表）→ 启动 Vite 前端，并打印访问地址。
按 `Ctrl-C` 一次性停掉全部服务（含 uvicorn 的 reload 子进程）。

```
游戏入口    http://localhost:5173
接口文档    http://127.0.0.1:8000/docs
```

其他用法：

```bash
./scripts/dev.sh --reset     # 先清空本地 SQLite 数据库再启动
./scripts/dev.sh backend     # 只启动后端
./scripts/dev.sh frontend    # 只启动前端
./scripts/dev.sh test        # 后端 pytest + 前端 vitest + 类型检查
./scripts/dev.sh help        # 帮助
```

### 端口配置

端口、数据库、密钥全部来自仓库根目录的 `.env.local`（模板见 `.env.local.example`）：

```ini
BACKEND_PORT=8000
FRONTEND_PORT=5173
DATABASE_URL=sqlite+aiosqlite:///./dev.db
JWT_SECRET=dev-only-secret-change-me-0123456789abcdef
```

改完直接重跑脚本即可。脚本会自动把 `CORS_ORIGINS` 同步成前端实际地址，
并把 `VITE_API_BASE` 注入给 Vite，因此换端口不需要改任何代码。

> 本机若已装 PostgreSQL，把 `DATABASE_URL` 改成
> `postgresql+asyncpg://用户:密码@localhost:5432/库名`，并在 `backend` 目录执行
> `.venv/bin/alembic upgrade head` 即可，脚本其余流程不变。

日志写在 `.dev-logs/backend.log` 与 `.dev-logs/frontend.log`。

---

## 服务器部署（Docker Compose）

```bash
cp .env.example .env          # 修改端口、数据库密码、JWT 密钥
docker compose up -d --build
docker compose logs -f backend
```

访问 `http://<服务器地址>:${FRONTEND_PORT}`（默认 8080）。

编排包含五个服务：

| 服务 | 说明 | 端口 |
| --- | --- | --- |
| `frontend` | nginx 托管前端静态资源，并把 `/api` 反代到后端（同源，无需 CORS） | `${FRONTEND_PORT}` → 80 |
| `backend` | FastAPI，入口脚本会等数据库就绪 → `alembic upgrade head` → 启动 uvicorn | `127.0.0.1:${BACKEND_PORT}` → 8000 |
| `coop-worker` | 远征（团队副本）推进进程，复用后端镜像 | — |
| `worldboss-worker` | 世界BOSS 推进进程（共享血量递减），复用后端镜像 | — |
| `db` | PostgreSQL 16，数据持久化在 `${POSTGRES_DATA_DIR}`（默认 `./data/postgres`，bind mount 到本机） | `127.0.0.1:${POSTGRES_PORT}` → 5432 |

> `coop-worker` / `worldboss-worker` 均为独立进程：缺少它们时远征 / 世界BOSS 不会推进（不影响其它玩法）。

### 端口与变量（`.env`）

```ini
# 对外端口
FRONTEND_PORT=8080     # 唯一需要公网开放的端口
BACKEND_PORT=8000      # 默认只绑定 127.0.0.1
POSTGRES_PORT=5432     # 默认只绑定 127.0.0.1

# 数据库
POSTGRES_USER=eorzea
POSTGRES_PASSWORD=请务必修改
POSTGRES_DB=eorzea

# 数据库文件在本机的存放目录（bind mount）
POSTGRES_DATA_DIR=./data/postgres

# 应用
JWT_SECRET=请替换为随机值（openssl rand -hex 32）
CORS_ORIGINS=http://localhost:8080

# 兑换码（留空关闭该功能；每个账号对同一码只能兑换一次）
REDEEM_CODE=
REDEEM_GOLD=0

# 管理员（留空 ADMIN_PASSWORD 即不启用；管理员不参与排行榜）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
```

`db` 与 `backend` 默认只监听 `127.0.0.1`，不对外暴露。
若要让后端 API 直接对外（例如前后端分离部署），把 `docker-compose.yml` 中
backend 的 `127.0.0.1:` 前缀去掉，并把 `CORS_ORIGINS` 设为前端实际域名。

前端镜像构建时通过 `VITE_API_BASE=/api/v1` 走同源请求；
若改为独立域名部署，用
`docker compose build --build-arg VITE_API_BASE=https://api.example.com/api/v1 frontend` 重新构建。

### 常用运维命令

```bash
docker compose ps                                    # 服务状态
docker compose logs -f backend                       # 后端日志
docker compose exec backend alembic upgrade head     # 手动迁移
docker compose exec db psql -U eorzea -d eorzea      # 进数据库
docker compose down                                  # 停止（保留数据）
docker compose up -d --build                         # 更新实例（数据保留）
```

### 数据目录与备份

数据库文件通过 bind mount 落在本机目录 `${POSTGRES_DATA_DIR}`（默认仓库内 `./data/postgres`），
不在容器里，因此**重新构建 / 重建容器不会丢数据**：

- 备份：直接复制该目录即可，例如 `tar czf pgdata-$(date +%F).tar.gz -C data postgres`；
- 恢复：`docker compose down` 后用备份替换 `./data/postgres` 再 `docker compose up -d`；
- 迁移服务器：把该目录一起拷到新机器上的相同相对路径即可；
- 换路径：改 `.env` 里的 `POSTGRES_DATA_DIR` 后 `docker compose up -d`（建议先把旧目录内容搬过去）。

> 注意：该目录已在 `.gitignore` 中排除，不要提交到仓库。
> Linux 上若 `db` 容器启动报数据目录权限错误，执行 `sudo chown -R 999:999 ./data/postgres` 后重启。

---

## 手动启动（不使用脚本）

```bash
# 后端
cd backend
python3.12 -m venv .venv && .venv/bin/pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -e ".[dev]"
cp .env.example .env
.venv/bin/alembic upgrade head          # 或设 AUTO_CREATE_TABLES=true 自动建表
.venv/bin/uvicorn app.main:app --reload --port 8000

# 前端
cd frontend && npm install && npm run dev
```

前端通过 `VITE_API_BASE`（默认 `http://localhost:8000/api/v1`）访问后端。

---

## 核心设计

### 权威边界

- 客户端逐帧模拟战斗并渲染（伤害浮动、技能 CD、日志），技能 CD 以 0.1s 节拍刷新展示。
- 有事件时才批量上报：`{regionId, elapsedMs, kills[], skillCasts[], bossKilled, died}`。
- **服务端是唯一权威**：金币/经验上限校验、图鉴解锁、进度推进全部在服务端完成。
  **装备只能通过抽箱获取**（BOSS 宝箱奖励除外），怪物与精英不掉落装备。
  客户端上报的击杀数与金币会被截断或拒绝（写 `audit_logs`）。
- **不采信客户端时间**：击杀额度按「理论击杀速率 × 时间」累积，时间窗口**只取服务端记录的真实间隔**
  （客户端上报的 `elapsedMs` 仅作参考，不参与计算）。否则改系统时间或用浏览器加速插件拉快动画帧，
  就能凭空放大击杀额度，等于把游戏加速。跨上报保留余额，短上报与空闲窗口不会误杀合法击杀。
- 本地模拟同样防加速：模拟时间按 `Date.now()` 的真实墙钟发预算，动画帧时间戳被篡改也不会被拉快。

### 不做离线收益

按需求确认，离开页面即暂停战斗，服务端不存在离线回补逻辑。

### 装备归属账号

装备挂在账号而非英雄上：解雇/替换英雄时装备自动卸下并保留在背包（PRD 招募 2.4）。

### 数值锚定

PRD 中怪物与英雄的成长曲线若直接采用会导致数值发散（BOSS 战长达数分钟）。
本项目改为**以「期望英雄」曲线为锚**推导怪物属性。期望英雄 = 等级匹配地区 + 已装备等级对应底材武器 + 5 技能职业：

```
monsterHp = refAttack(level) × refGearAttackMultiplier × (refPotencyPerSecond / 100) × refDamageMultiplier × targetKillSeconds × template.hp
```

系数全部集中在 `shared/data/monsters.json:reference`，改一处前后端同时生效。**击杀手感以「命中次数」为准**（`monsters.json:templates[].multipliers.hp`）：英雄开局技能全部就绪，大招会前置爆发，短战斗实际由前几发决定，只看稳态耗时无法反映手感。实测（真实客户端模拟器 `BattleSimulator`，等级匹配的期望英雄打满整轮地区）目标为 **普通怪约 3 下 / 精英怪约 5 下 / 关底 BOSS 约 10 下**：

| 场景 | 单怪 | BOSS |
| --- | --- | --- |
| 起始英雄（Lv1 + 铁制长剑）打地区 1 | 11s | 42s |
| 期望英雄（等级匹配稀有武器） | 5.5-5.9s（约 3 下） | 17-21s（约 10 下） |
| 神话武器（Lv100 打地区 40） | 3.6s | — |
| 裸英雄（无武器） | 19-22s | ~87s |

小怪阶段总时长约 60-180 秒（8-36 只，血量上调后约为旧版 2 倍）；英雄装备不足时会被击杀，从而触发「阵亡重置」机制。

> 早期版本只按「裸普攻」标定参考曲线，玩家只要转职 + 穿装，DPS 就是锚定的 4-8 倍，导致秒杀一切（0.7 秒/只）。随后改为按期望英雄锚定稳态耗时，但短战斗仍被开局大招主导——实测普通怪 1-2 下即死、BOSS 2-3 下。现将 `templates[].multipliers.hp` 按上述命中次数重新标定（攻击 / 防御倍率不变，只有血量上调）。

### 战力平衡 v2

权威配置为 `shared/data/balance.json`（版本 `2.0.0`）。启动时严格校验；损坏、缺字段或不安全的参数会记录错误，并使用 `backend/app/services/balance_defaults.py` 中的安全默认值。修改配置后重启后端，客户端使用会话返回的倍率。

战力按 `cap × (1 − exp(−raw / cap)) × weight` 累加，并区分进攻、防御、续航贡献。只计职业实际使用的攻击属性，金币、经验、在线时长及重复次数不直接贡献战力。尚未实现的穿透属性不计分。角色面板和 `/api/v1/game/state` 的 `powerAudit` 可查看原始属性、有效属性、分项贡献及机制标记。装备估值仍服务于出售与重造，不等同于角色战力增量。

普通副本开放低战力挑战。差距 `deficit = clamp((推荐战力 − 玩家战力) / 推荐战力, 0, 1)` 同时作用于伤害、承伤、治疗/护盾、资源回复、技能冷却、机制间隔/蓄力窗口和奖励。初始最大惩罚分别为 −35%、+30%、−25%、−20%、+15%、−20%、−40%；命中影响最多 10%，最终命中不低于 80%。地区战斗也使用服务端下发的战力差距倍率，旧 `levelPenalty` 仅保留兼容函数，不再作为实际战斗规则。

「高难副本」页 BOSS 属性**固定锚定副本的 `challengeLevel`（目标等级）**，不再随玩家等级或战力动态变化：英雄低于目标等级时按 `regions.levelPenalty` 受到等级压制（命中/输出下降、承伤提升、防御衰减），因此「刚好卡进入等级」不足以通关，需要练到目标等级并配好装备（例：目标 Lv.67 的副本要到 65-70 级才打得动）。进入还与装备质量、战力、主攻击、双防挂钩（绝* 为硬门槛，极* 可低战力进入练习）；正式通关还要求输出检查、防御检查及服务端时长校验。每个副本每天有 `dailyRewardClears` 次奖励通关（首通计入），超出后仍记录通关与最快用时，但不再产出金币/经验/宝箱，用于防止反复刷取。高难结算与地区同源，吃装备词条、药水/食物与彩蛋被动的经验/金币加成。每次尝试保存版本、资格与惩罚快照；过期无快照会话需重新挑战。

新地区必须同时满足战力、装备质量、前一地区 BOSS 通关、主攻击、双防（**不再要求机制试炼**）。初始地区 1 是默认开放的新手入口。默认 `migration = "recalculate"`：列表、进入、推进、当前地区和战斗上报均重算资格，旧解锁标记不能绕过新条件；角色、装备、奖励、历史通关与最快记录均保留。`new_unlocks_only` 可保留已解锁地区，后续新地区仍必须通过全部条件。

管理员接口 `GET /api/v1/raid/telemetry` 按副本返回最近 30 天装备达标且尝试过高难的去重玩家首通率、首次进入人数、达标人数、首通人数、机制/输出/防御失败率、平均尝试次数和战斗时长。空样本返回 `null` 首通率；默认至少 30 人、连续三个按天滚动窗口同方向超出 45%–55% 才返回调参方向。机制死亡明细是客户端上报且经合法技能 ID 过滤的诊断信息，不作为通关资格凭据。停止及放弃挑战也保留在尝试样本内。

45%–55% 是上线后的观测目标，合成样本测试不能证明真实玩家首通率已达标。运维应依据滚动窗口，仅修改版本化门槛/战斗参数，不改历史通关记录；发布不兼容资格规则时提升 `version`。

部署前执行数据库迁移：`cd backend && .venv/bin/alembic upgrade head`。迁移 `g4b6c8d0e2f4` 添加挑战快照与结果列，`c3d4e5f6a8b9` 移除机制试炼表，`l9d1f3a5c7e9` 添加副本每日奖励计数列，均不重写历史数据。已有角色资格按请求即时重算，无需批量重写装备或奖励。

---

## 已实现系统

| 系统 | 说明 |
| --- | --- |
| 装备品阶 | 6 品阶（白/绿/蓝/紫/橙/红），倍率、词条数量、Debuff 概率、箱内概率 |
| 装备栏位 | 11 格（主手 + 5 防具 + 项链/耳环/手镯/戒指 I/戒指 II），初始全部解锁 |
| 装备属性 | 底材固定属性 × 品阶倍率 × ±20% 浮动；副属性 1-3 条；0-4 个 Buff/Debuff |
| 装备变体 | 战斗底材按「族 × 档位 × 变体」展开（9 档 Lv.1/20/40/60/80/85/90/95/100），同档多套装备：变体由 `minTier` 逐档解锁（档位越高种类越多，Lv.80 防具 8 种、舞者武器 3 种）；各变体的**副属性池（种类）与档位缩放（数值上下限）**都不同，图标以色调 + 角标区分。见 `shared/data/base-items.json:variants` |
| 触发效果 | 词条支持概率触发（proc）：**灼烧 / 淬毒 / 裂伤**（命中概率给目标挂 DOT）、**疾风**（命中概率叠限时攻速）、**凋零 / 致盲 / 破防 / 缓速 / 眩晕**（命中概率施加敌方减益）、**反震 / 复仇 / 庇护 / 坚毅 / 格挡**（受击概率触发）。概率由词条值承载，效果量在 `combat.json:proc` / `combat.json:equipEffects.proc`；前端 `battle.ts` 结算，后端 `combat_model.proc_dps_bonus` 计入期望。**攻速**（sks 副属性 / 迅捷 / 疾风）会缩短 GCD 与普攻间隔 |
| 附魔 12 类 | 战斗词条按 `terms.json:categories` 分为 12 类：常住属性 / 攻击触发 / 异常 / 受击 / 防御 / 条件 / 动态成长 / 资源转换 / 联动 / 风险代价 / 特殊机制 / 累计触发（另设「收益获取」承载金币 / 经验类）。新增机制的数值与阈值集中在 `combat.json:equipEffects`（proc / conditional / growth / convert / charge / special），前端 `battle.ts` 结算、后端 `combat_model.py` 仅镜像影响期望 DPS 的部分（其余只影响生存与资源，与凋零 / 失明同待遇）；风险代价类用 `cost` 字段声明副作用（落库 `value = round(主值 × ratio)`），`desc` 用 `{v}` / `{c}` 两个占位符。词条图鉴可按「类别」筛选。见 `docs/enchant-terms-expansion-design.md` |
| 三属性系统 | 暴击（二次收益）/ 直击（固定 125%）/ 信念（恒定乘算），含等级缩放分段 |
| 主属性 | 力量 / 敏捷 / 智力，非主属性收益 50%，职业匹配加成 |
| 战斗 | 自动释放技能、GCD 1.5s、MP 消耗与回复、技能优先级。**普攻**（零耗蓝兜底）威力为 `combat.json:basicAttackPotency`（当前 50%，刻意低于技能），独立计时器出手、受攻速缩短，与技能完全独立；因威力低于 100%，不享受彩蛋「战斗爽」 |
| 关底 BOSS 斩伤上限 | 单次命中对地区关底 BOSS 的伤害不超过其最大生命的 `bosses.json:maxHitDamagePct`（当前 20%），避免开局技能全就绪时的爆发 / 暴击大招单次秒杀 BOSS。仅作用于地区关底 BOSS，不影响高难副本与联机（客户端加上限后比服务端理论模型更保守，不会导致上报被拒） |
| 职业 | 21 个战斗职业，每职业 5 个技能（含 60-120s 大招） |
| 地区关卡 | 40 个地区、6 个篇章；小怪计数 → BOSS → 解锁下一地区；阵亡重置 |
| 难度等级 | 「战斗」页可在**已解锁**难度间自由切换（0–15）。**周目制**：进入新难度后 40 个地区重新锁定，需再次 1→40 通关，通关当前难度最后一个地区的关底 BOSS 才解锁下一难度。怪物数值按**加法**放大（生命/防御/经验每级 +100%、攻击每级 +50%、金币每级 +10%），玩家攻击/防御按**乘法**缩小（`0.85^N` / `0.9^N`）。配置见 `combat.json:difficulty`，进度按 `region_progress.difficulty` 隔离；难度只作用于地区战斗，不影响高难副本 / 远征 / 世界BOSS |
| 战力差距 | 按推荐战力差距施加多维软惩罚，倍率由服务端下发；见「战力平衡 v2」 |
| 抽箱 | 6 种箱子、连抽档位 **单抽/十连/50 连/100 连**（50 连需一次性 500 万金币解锁、100 连 2000 万，账号级解锁后所有箱子通用）、品阶概率、10/50/200 抽保底。抽奖动画按品阶给物品描边着色（`ChestReel`）；50/100 连用网格波浪揭晓。档位与解锁价见 `shared/data/chests.json:drawCounts` |
| 合成 | 16 件同品阶同类 → 高 1 阶，含一键合成与预览；**合成后弹窗展示本次产出**（消耗件数 / 手续费 / 逐件属性） |
| 出售 | 底价 × 品阶系数 × 属性系数（饱和封顶）× 词条价值 × ±10%，支持批量与自动出售；**抽箱中被自动出售的装备仍参与抽奖动画，出售结算在结果页展示**（未入背包） |
| 重造 | 两种模式：**彻底随机**（基础属性+副属性+全部 Buff/Debuff 重洗，等同重获该装备）与**基于当前**（属性与词条都在现有值附近 ±区间宽度×10% 内独立浮动，可升可降）；保留品阶/类型；**消耗随重造次数递增**（不封顶） |
| 附魔 | 两种模式同重造（彻底随机 / 基于当前）；**Buff 有普通/稀有/太古品质**（稀有 1.0% / 太古 0.1%，太古以 🌟 标记），**Debuff 不参与品质判定（恒为普通）**；支持自动追梦 |
| 属性区间 | 装备详情每条形如 `暴击 +85【48-144】`（基础属性 ±20% 带、副属性可达区间）；太古显示 `值🌟` 且金色 |
| 装备标签 | 玩家自建**命名 + 调色板颜色**的标签（最多 30 个/账号，名称 ≤16 字），一件装备可贴多个；背包可按标签多选筛选（**命中任一即显示**）。标签按账号持久化（`item_tags` 表 + `items.tag_ids`） |
| 副本 | BOSS 属性**固定锚定 `challengeLevel`（目标等级）**，不再按战力缩放；低于目标等级受等级压制。绝* 要求等级/装备/战力/关键属性（输出/防御检查），极* 可低战力进入练习。每个副本每天有奖励通关次数上限（防刷）。门槛见 `balance.json`，遥测规则见「战力平衡 v2」 |
| 副本压力 | BOSS 的攻击力与倍率经**真实模拟器（BattleSimulator）标定**：达标装（达到该等级 `requiredPower` 的装备）通关后约剩 40%-60% 生命；绝* 入口按高线拦下不达标玩家，未练到目标等级的玩家则被等级压制挡下 |
| 副本技能 | 「高难副本」页每个 BOSS 都带**共享技能池（13 个技能）**：客户端为每个 BOSS 维护**一个共享 CD（6s）**，到点后**随机抽一个**释放（高威力伤害 / 持续伤害 / 增伤 / 减伤 / 减速），不再各技能独立 CD。地区战斗不受影响 |
| 英雄招募 | 酒馆刷新与金币招募，资质 6 阶，三维按偏向分配；**任何「太古属性英雄」都必定为神话（红色）资质**（保底或自然触发皆然，计算前总点数落在 220-260）；**太古属性保底**：每 500 个候选必出一次太古（进度在酒馆页展示） |
| 图鉴 | 装备（按底材 × 品阶）、怪物、词条（战斗装备 + 生产/采集专用装备，各按普通/稀有/太古三档解锁）、材料（采集材料 + 半成品）、鱼获（普通鱼 / 鱼王 / 鱼皇，按钓场） |
| 排行榜 | 等级榜 / 关卡榜（必做）+ 战力榜 / 金币榜 / **游玩时间榜**，每 5 分钟服务端刷新；昵称后以 0.6 透明度显示 `#登录账号`（如 `ABCD#zhangsan`）便于区分重名（昵称可重复，账号唯一）。**点击任意玩家可查看其当前装备**（只读，仅已装备栏位；需登录，封禁/无英雄账号返回 404）。弹窗内含「战斗装备 / 生产采集装备」页签，可查看对方的生产 / 采集专用装备，并显示其累计游玩时间 |
| 远征榜 | 排行榜页新增**远征榜**页签：按**副本下拉筛选**，列出各玩家该副本的**最快通关时长**（越小越快，升序），行内展示该次通关的**阵容**；点击任意行弹出**分角色输出图表**（伤害 / 承伤 / 治疗 / 死亡 / 最低血量，手写条形图）。个人 / 离线 / 在线统一榜，行内标注模式与是否含克隆。数据来自服务端权威模拟：每次通关由 `coop_worker` 写入 `coop_records`（每个真实参战账号一行），**实时聚合**、刚通关即可见 |
| 世界BOSS | 全服共享血量的 BOSS「黄金巴哈姆特」（初始 **20 亿**血量、第一阶段攻击力 **20000**、击杀后 **5 小时**刷新）。入口「世界BOSS」页。所有在线玩家各自上阵最多 **8 名英雄**同时削弱同一血量：由独立进程 `worldboss-worker` 服务端权威推进（`worldboss_engine`，100ms tick），BOSS 普攻对全体存活英雄、**技能按固定间隔随机独立释放**（技能池 ≥20 个），每个英雄**独立死亡 / 复活**（`worldboss.json:reviveSeconds`）。**英雄需 80 级以上**，80–99 级输出/治疗按线性削弱，满级（100）解除。**阶段（P1→P2→P3）**按全服剩余血量占比自动进入（≤60% 进 P2、≤30% 进 P3）：血量越低 **BOSS 防御越厚**（英雄输出按 `1/defenseMultiplier` 折算）、**技能威力越高**（`skillPotencyMultiplier`，**不影响普攻**），血条上标出阶段分界。按**击杀周期**结算：每轮按侧边**实时总伤害榜**名次发「**绝境龙神**」系列（第 1 名 20 件，递减至第 10 名 1 件，第 10 名及以后凡达标均 1 件；**入榜门槛总伤害 ≥ 500 万**）；**点击榜单任一行可展开**查看该玩家本周期**各英雄的伤害与百分比**（堆叠占比条 + 明细）。血量 / 周期 / 总伤害榜**不随版本更新重置**。配置：`shared/data/worldboss.json`、`shared/data/exclusive-equipment.json` |
| 绝境龙神系列 | 世界BOSS 专属掉落（**不可抽奖 / 打造 / 合成**）：固定**红色（神话）品质**、固定 **100 级**，覆盖**武器每职业 1 件 + 防具每部位 2 件 + 饰品每部位 2 件 = 39 件**，各带独立像素建模（`npm run gen:icons`）。主属性 / 副属性上限高于同等级其他装备（锚定值约 1.4×）。**可重造 / 附魔**，但成本 ×`economy.json:exclusiveCostMultiplier`（默认 10，远高于其他装备）。已入图鉴（来源标注 `worldBoss`）并带「绝境龙神」标签 |
| 游玩时间 | **累计在线时长**：由战斗 / 采集 / 生产 / 钓鱼 / 副本的**服务端**上报窗口累加（`users.play_ms`，毫秒精度）。客户端时间不可信、离开页面即停，因此不含挂机与离线时间。排行榜新增**独立「游玩时间榜」**，同时**每个榜单的行内都显示游玩时间** |
| 新手指引 | 15 步可操作引导：自动前往对应页面、定位并高亮操作区域；PC 右侧栏、手机可收起底部面板（预留空间），不遮挡操作弹窗。可先了解后继续、跳过（无奖励）及设置内重播（奖励只发一次） |
| 个人信息 | 设置页支持修改昵称（1–32 个字符），同步页头与排行榜；登录账号不变 |
| 好友 | 每个账号有唯一**好友码**（8 位易读字符，注册 / 迁移时生成）；凭好友码**申请 + 对方同意**建立好友关系。好友列表显示**在线状态**（绿=在线 / 灰=离线，基于前端 20s 心跳与服务端 `users.last_seen_at`，45s 内有心跳视为在线），可查看对方装备、删除好友。好友间可**金币转账**：转账方支付 X，收款方实收 `X − ⌊X×10%⌋`（手续费销毁回收），另有单笔上下限与每日累计上限；参数见 `shared/data/economy.json:transfer` |
| 聊天室 | 单一公开大厅，登录后可看可发，实名展示 `昵称#登录账号`（与排行榜一致；**管理员仅显示「管理员」+ 徽章，不回显其登录账号**）。**仅文本**（单条 ≤200 字），**不保留聊天记录**：服务端只保留最近 10 分钟的消息（`services/chat.RETENTION_SECONDS`），窗口外的历史永不返回；**发言限频 20 条 / 60 秒**（`guard_rate`）防刷屏。实时推送用 WebSocket（`/chat/ws`，短时效一次性 ticket 鉴权），参照远征的 DB 轮询广播，多 worker 安全。管理员可在聊天室**发布公告**（高亮展示，不受发言限频）；公告是「不保留记录」的例外，**置顶在聊天室顶部且长期保留**，不随 10 分钟滚动窗口消失 |
| 兑换码 | 码与奖励金币由 `REDEEM_CODE` / `REDEEM_GOLD` 配置，每个账号对同一码只能兑换一次；另按客户端 IP 限流并限制**同一 IP 可兑换同一码的账号数**，堵住「多开小号刷码换金币」 |
| 账号 | 账号密码注册登录 + JWT |
| 反滥用 | 按客户端 IP 的滑动窗口限流（注册 / 登录 / 兑换），防多开小号与撞库；阈值由 `REGISTER_PER_IP_*` / `LOGIN_PER_IP_5MIN` / `REDEEM_*` 配置，见 `backend/.env.example` |
| 管理员 | 账号名与密码由 `ADMIN_USERNAME` / `ADMIN_PASSWORD` 配置（启动时创建/同步，改密重启生效）；**不参与排行榜**；登录后可在「管理」页搜索用户、重置其密码、**封禁 / 解封账号** |
| 生产 / 采集 DLC | 见下节「生产 · 采集 DLC」 |

---

## 生产 · 采集 DLC

能工巧匠（8）与大地使者（3）共 11 个非战斗职业，与战斗并列的第四套玩法。配置全部在
`shared/data/dohdol-jobs.json`、`dohdol-levels.json`、`materials.json`、`gather-nodes.json`、
`recipes.json`、`dohdol-equipment.json`、`fish.json`、`consumables.json`、`titles.json`。

| 系统 | 说明 |
| --- | --- |
| 采集 | 「采矿工 / 园艺工」按地区采集：**不同地区采集不同材料**——每个地区 2 件专属材料（矿物 + 植物，共 80 件、**跨地区不重复**，FF14 风格命名），另可采到少量通用材料，故每地区可采 ≥3 种；材料不随地区等级递增、仅按种类区分。后台自动循环，产出由服务端按采集等级 + 专用装备 + 药水加成结算；经验 = 采集次数 × 基础 ×（1 + 采集经验加成），加成逐条来自专用装备（含「博识」词条）与药水 / 食物 |
| 生产 | 8 个能工巧匠按配方制造材料 / 半成品 / 战斗装备 / 专用装备 / 药水食物。配方按**生产等级**解锁。经验 = 配方基础 `xp` ×「品阶经验系数」×（1 + 制造经验加成），制造经验加成逐条来自专用装备（固定加成 +「灵感」词条）与药水 / 食物「经验获取」；其中装备按实际抽到的品阶乘 `recipes.json:equipment.xpRarityMultiplier`（普通 1.0 → 神话 3.0，**品阶越高经验越多**），材料 / 半成品 / 消耗品无品阶，恒按 1.0 |
| 制造数量 | 每个配方提供 **「制作 X 个」**（X 由玩家填写）与 **「制作全部」** 两个按钮：「X 个」= 制造 `min(X, 当前材料上限)` 件；「全部」= 按当前材料能制造的上限件数。目标件数由**服务端**结算（`activity_sessions.target_actions`），达成后**自动结束会话**（前端停止上报并提示「制造完成」）；材料不足时返回 400「材料不足，无法制造」 |
| 制造装备 | **恒为「高品质」**：属性区间整体上移（`recipes.equipment.highQualityMultiplier`）且**必带 1 条太古词条**；品阶按进度动态抽取（见「制造品阶概率」）。同品阶下强于抽奖装备 |
| 制造品阶概率 | 像抽奖一样随进度提升：`t = Σ 权重 × min(当前值 / 参考值, 1)`（权重合计 = 1），分布 = 基准 `rarityWeights` ×(1−t) + 目标 `targetWeights` × t。**七项来源** = 英雄等级 / 通关地区数 / 生产等级 / 专用装备「制造品阶幸运」合计 / 制造品阶概率药食 / **远征通关** / **高难通关**（后两者按难度加权、**仅计首通**），权重与参考值见 `recipes.json:equipment.rarityScaling`。每个参考值即该来源的**真实最大值（含太古词条）**，故**全部来源满 → t=1 → 神话概率 20%（硬上限，永不超出）**，任何单项拉满都不足以到顶。生产页实时显示当前玩家各品阶概率，点「?」看公式与代入值 |
| 专用装备词条 | 生产 / 采集专用装备也带 **Buff/Debuff 词条**（池在 `dohdol-equipment.json:terms`，仅限专用栏位，不污染战斗词条与词条图鉴）：如「品阶幸运」(制造品阶概率 +)、「品质洞察」、「巧手」、「灵感」(制造经验)、「丰收」、「渔王的直觉」、「博识」(采集经验) 等，品质规则与战斗词条一致（普通/稀有/太古）；含负面「品阶失衡 / 品质钝化 / 歉收」等。词条与固定加成一起计入 `equipped_bonus` |
| 专用装备 | 生产 / 采集各有独立栏位（主手工具 + 副手工具 + 5 防具），**仅能通过生产获取**，只影响采集产量 / 制造品阶与品质 / 制造速度 / 钓鱼概率，**不参与战斗结算、战力榜、地区与副本门槛**（`stats.aggregate_equipment` 显式跳过，且不能重造 / 附魔） |
| 等级 | 所有能工巧匠共用**生产等级**，所有大地使者共用**采集等级**（上限 **100**，与英雄一致）；由制造 / 采集 / 钓鱼获得经验。专用装备覆盖 Lv.1-100（11 档 × 变体），采集点等级门槛按地区铺满 1-100。经验曲线见 `dohdol-levels.json:expCurve`（base 80 / growth 1.045，1→100 总经验约 13.7 万），配合上调后的配方经验，生产等级可在**约 10 小时内**练满 |
| 钓鱼 | 捕鱼人为特殊场景：按地区钓场自动抛竿，普通鱼随机种类与尺寸；钓起指定普通鱼开启「**捕鱼人之识**」（30-60s，随鱼种），期间才有小概率出现**鱼王 / 鱼皇**（每钓场各 1 条，鱼皇概率低于鱼王）。钓全所有鱼王 / 鱼皇各解锁一个**称号** |
| 药水 / 食物 | 炼金术士制药水（60s，效果强）、烹调师制食物（1800s，效果弱），**可共存**、手动使用。分 **I / II / III 三档**：档位越高效果越强（×1 / ×2 / ×4，如经验获取 +25/+50/+100%），但**单个物品的持续时长不变**；II / III 档配方分别需生产等级 40 / 80，并用对应地区的采集材料（`flora17`/`ore17`、`flora32`/`ore32`）。**同一件连续使用时长叠加**（在剩余时间之上再累加一份时长）；换成同槽位的另一种则重置为新的一份时长。效果覆盖经验/金币（经验加成对战斗 / 生产 / 采集 / 钓鱼均生效）、**抽箱品阶概率**、**制造品阶概率**、制造品质概率、捕鱼人之识时长、采集产量；**不影响战力与门槛** |
| 排行榜 | 新增**两个独立榜单**：**钓鱼种类榜**、**钓鱼数量榜**（只有有钓鱼记录的玩家上榜）。这两个榜**实时**从 `fish_records` 聚合（刚钓完就能看到，不等 5 分钟缓存）；种类榜还会按**普通 / 鱼王 / 鱼皇**拆开显示种类数。缓存榜（含游玩时间榜）仍走 5 分钟缓存。行内展示称号徽章与游玩时间 |
| 物品出售 | **采集材料 / 半成品 / 鱼获 / 药水食物均可卖给系统换金币**（单价见各 JSON 的 `sell` 字段，按地区档位/稀有度递增）。见 `POST /dohdol/sell`；采集页、生产页、钓鱼页、背包都有「出售 / 全部出售」按钮 |
| 互斥 | 战斗 / 采集 / 生产 / 钓鱼 **四活动互斥**，服务端在会话启动时强制结束其他会话 |
| 生产 / 采集日志 | 采集页与生产页各有一块日志面板，按每次服务端结算追加「本次获取数量」与「获得经验」两行；经验行展示结算基础、品阶系数（生产装备）与**逐条经验加成来源**（专用装备名 / 词条名 / 药水食物名 + 各自百分比），最多保留最近 120 条，开始新会话时清空 |

服务端权威边界与战斗一致：客户端只定时上报，**产量与随机结果全部由服务端结算**；结算窗口只取
服务端时钟（`dohdol_util.window_seconds`，单次上限 30s），离开页面即暂停，**不做离线收益**；
药水 / 食物到期同样以服务端时钟判定。新表见迁移 `f2a4b6c8d0e2`，生产目标件数列见 `a7c9e1b3d5f7`。

采集 / 生产 / 钓鱼的开始与上报响应都会带上 `cycle`（单次动作耗时 + 未结算余额 + 服务端时间戳 `at`），
前端用**半 RTT 校正**插值画进度条——进度条跑满的时刻与服务端产出时刻对齐，且不依赖客户端时钟；
进度条只是展示，结算仍以服务端为准。鱼类名称参考《最终幻想 XIV》（鱼王 / 鱼皇每个地区各一条，名称不重复）。

内容（材料 / 采集点 / 专用装备 / 鱼类 / 消耗品 / 配方 / 称号）由
`scripts/gen-dohdol-data.py` 生成，改完重跑即可（与 `scripts/gen-icons.mjs` 同为内容生成器）：

```bash
python scripts/gen-dohdol-data.py
```

---

## 物品像素图标

每个物品都有 1 张 16×16 像素 PNG（`frontend/src/assets/icons/<id>.png`），由 `scripts/gen-icons.mjs` 生成，
共 **1938 张**：战斗装备 935（30 个部位底形 × 9 个档位 × 变体）+
采集材料 / 半成品 98 + 生产·采集专用装备 623 + 鱼获 240 + 药水食物 42。

```bash
npm run gen:icons
```

- 战斗装备：主体配色 = 材质档位色（铁制 → 星辉）；同档位不同部位靠底形区分，同部位不同档位靠装饰戳 + 材质色区分。
- **变体**：同档位多套底材（见 `base-items.json:variants`），名称中段为 FF14 官方职能装备词缀（御敌/强袭/制敌/游击/精准/治愈/咏咒），装备名 = 档位 + 职能 + 部位（如「精金强袭铠甲」），与 FF14 命名惯例一致。除副属性池与档位缩放不同外，图标按变体名称的**职能**做色调混合（强袭=红、制敌=紫、游击=绿、精准=青、御敌=铜、治愈=金、咏咒=蓝）并在右上角加职能角标 —— 同一职能无论武器 / 防具 / 饰品都取同一色，因此图与名始终对应、同档不同变体一眼可辨。
- 材料 / 半成品 / 专用装备 / 鱼获 / 消耗品：按**形状族**（矿块·晶石·岩石·粉末·原木·草药·花·果实·蘑菇、木板·锭·板材·宝石·玻璃·皮革·布料·瓶·面袋、小鱼·鱼王·鱼皇、药瓶·餐盘、锤·锉·镐·斧）+ **名称关键词配色**（铜=铜色、松木=木色、紫水晶=紫…）组合，再用 id 哈希做确定性微调；专用装备用暖棕（生产）/ 青绿（采集）档位色，鱼按普通 / 鱼王 / 鱼皇上色并按地区生态色微调。
- **品阶 / 稀有度不烘焙进图片**，由 `ItemIcon.vue` 用 CSS 描边 / 光晕施加（战斗装备 `variant="full|lite"`，材料等无稀有度物品 `variant="plain"`，鱼按普通 / 鱼王 / 鱼皇给光晕色）。
- 单张约 139 字节、合计约 263 KB，低于 Vite 默认 `assetsInlineLimit`，构建时全部内联为 data URI ⇒ **渲染物品时零图片请求**。
- 想改某件物品的造型，改 `gen-icons.mjs` 里的 `SHAPES` / `ORNAMENTS` / `TIER_PALETTES` / `KEYWORD_COLORS` 再重跑即可。
- 生成器与 `shared/schema` 的 `expandBaseItems()` 是两份 id 规则实现，由 `frontend/src/utils/icons.spec.ts` 一致性测试锁住（断言图标集合 == 战斗底材 ∪ 材料 / 半成品 / 鱼获 ∪ 专用装备 ∪ 消耗品）。
- 图标已接入**采集页 / 生产页 / 背包 / 装备栏（含他人装备弹窗）/ 鱼获页 / 图鉴（材料、鱼获）**；采集页另有「**各地区产出总览**」，展开后逐地区（含未解锁，置灰）列出采矿工 / 园艺工各自的可采材料（图标 + 名称 + 权重概率）。
- 内容生成器 `scripts/gen-dohdol-data.py` 改完材料 / 配方 / 鱼后，需再跑一次 `npm run gen:icons` 同步图标。

---

## 与 PRD 的差异说明

| 项 | 说明 |
| --- | --- |
| 装备栏位 | PRD 文字写「10 格」但列举为 11 格，按确认实现为 **11 格**（含双戒指） |
| 栏位解锁 | PRD 2.10.3 的「栏位解锁消耗」取消，初始 **全部解锁** |
| 离线收益 | PRD 2.7.4 作废，**不做离线结算** |
| 装备获取 | PRD 2.7.2 的「怪物概率掉落装备」取消，装备**仅通过抽箱获取**（BOSS 宝箱奖励保留） |
| BOSS 属性倍率 | PRD 4.2 写 HP 为小怪 50-80 倍，与 8.5「BOSS 战 10-30 秒」矛盾；改取可玩倍率 |
| 暴击率来源 | 三属性系统已合并「暴击率/暴击伤害」，敏捷不再提供暴击率 |
| 技能完整表 | PRD 仅给出 5 个职业示例，其余 16 个职业按同格式补全，数值为占位待平衡 |
| 怪物数值 | 改为锚定式推导，并按「命中次数」重标定血量（普通怪 3 下 / 精英 5 下 / BOSS 10 下），见上文「数值锚定」 |
| 起始武器 | PRD 2.5.1 写「初始英雄为冒险者、仅普攻」；因怪物强度按期望英雄锚定，裸英雄会卡死在新手阶段（地区 1 单怪 13.8s，存活期内打不满 8 杀），故开局自动装备一把**铁制长剑**（骑士），见 `heroes.json:initialHero.starterWeapon` |
| 战力差距 | 按推荐战力差距施加多维软惩罚，倍率由服务端下发；见「战力平衡 v2」 |
| 出售价格 | PRD 出售 3.2 的「×(1 + 属性评分/100)」随等级线性无上限增长，而箱子价格是固定值 → 高等级「买箱卖装备」稳赚。改为**饱和封顶**的属性系数（1 → 1+`attrBonusMax`），并重定品阶系数，使各箱期望卖价仅约为箱价的 20%-60%，只有抽到高品阶（约 5% 概率）才有赚头。见 `shared/data/economy.json:sell`、`rarities.json:sellCoef` |
| 雇佣资质系数 | PRD 招募 2.3 的资质系数为 1/2/5/15/50/200，神话资质在英雄 30 级时需 86 万金币，实际不可达；改为 **1/2/3/6/12/30**（`baseRecruitCost` 仍为 1000），神话资质 30 级降到 12 万。见 `shared/data/talents.json:talents.*.recruitCoef` |
| 太古保底与「太古⇒神话」 | PRD 招募没有保底。本项目给英雄的「太古属性」（0.1% 概率）加**保底**：连续 `ancientPityCount`（500）个候选未出太古时，下一个必出；计数在刷新 / 招募 / 十连 / 注册时推进，出太古后归零并落库在 `tavern.ancient_pity`。并且**任何带太古属性的英雄都必定为神话（红色）资质**（保底或自然触发皆然）——因此太古判定先于点数抽取，总点数落在神话区间 220-260（太古 ×1.25 计算后总值可超出该区间）。见 `shared/data/talents.json`、`backend/app/services/recruiting.py:generate_candidate` |
| 重造 / 附魔消耗 | PRD 重造 4.2 / 附魔 5.2 最高档为 60 万 / 300 万金币。本项目按「品阶 × 物品等级」定价：1 级基准价下调为**最高 3 万 / 5 万**（品阶递增、附魔始终比重造贵），再乘**等级系数** `1 + (2/27) × (物品等级 − 1)`（1 级 ×1、100 级 ×25/3 ≈ 8.33，用整数分数结算避免截断）。故 **100 级神话「基于当前」首造 = 30000 × 3 × 25/3 = 750,000**。见 `shared/data/rarities.json` 的 `refineCost` / `enchantCost`、`shared/data/economy.json:refine/enchant.costLevelGrowthNum/costLevelGrowthDen` |
| 重造递增 | PRD 未定义；本项目新增「每次重造在基准价上叠加 25%（不封顶）」，防止同一件装备无限重造刷属性。见 `shared/data/economy.json:refine.costGrowthPerRefine` |
| 重造/附魔「基于当前」 | PRD 未定义；本项目为两系统各设两模式。**彻底随机** = 全部重洗（重造洗基础+副属性+全部词条，附魔洗全部词条）。**基于当前** = 每条属性/词条在现有值附近按**当前值的百分比**独立浮动（`新值 = 当前值 ×(1 + U(−basedOnCurrentDownPct, +basedOnCurrentUpPct))`，默认 −5%~+10%），可升可降并夹回合法区间（旧实现是「最优值搜索、总值保底不降」，已废弃）；另外**已有太古词条数不减少**，且每次有 `basedOnCurrentAncientUpgradeChance`（5%）概率把一条普通 Buff 升为太古。重造与附魔都支持一次连做多次（请求体 `times`）。见 `shared/data/economy.json:refine/enchant`、`backend/app/services/item_factory.py:float_near_current / _roll_terms_based_on_current` |
| Debuff 品质 | PRD 附魔 5.3 的稀有/太古对 Debuff 会放大负面数值（更差）。本项目**取消 Debuff 的稀有/太古**：Debuff 恒为普通，仅在随机池内随机；稀有/太古只保留给 Buff（与副属性）。见 `shared/data/economy.json:debuffQualityEnabled` |
| 属性区间展示 | PRD 图鉴 3.4 只给底材展示理论范围，装备详情不展示。本项目让**装备详情**也显示「当前值【区间】」（基础属性 ±20% 带、副属性可达区间）；太古值超出区间，以 `值🌟` 金色展示。区间由 `baseId+品阶+属性` 推导，不落库（无迁移）；生产/采集专用装备的基础属性同样展示区间（`dohdol_base_attr_range`，其数值也按 ±20% 浮动生成） |
| 制造品阶概率 | PRD 生产部分未定义品阶概率（原实现为固定权重 白30/绿30/蓝22/紫12/橙5/红1）。现改为**随进度动态**：`t = Σ 权重 × min(值/参考值,1)`，分布 = 基准 ×(1−t) + 目标 ×t；**七项来源** = 英雄等级 / 通关地区数 / 生产等级 / 专用装备 `craftRarityPct` 合计 / 制造品阶概率药食 / **远征通关** / **高难通关**（后两者按难度加权、仅计首通）。各参考值为该来源**真实最大值（含太古词条）**，故**全部拉满 → t=1 → 神话 20%（硬上限）**，单项拉满不足以到顶。见 `shared/data/recipes.json:equipment.rarityScaling`、`item_factory.craft_rarity_luck/craft_rarity_distribution`；回归保护 `tests/test_dohdol.py:TestCraftRarityScaling` |
| 抽箱品阶概率来源 | PRD 只按箱子类型给固定品阶概率（2.8.2）；原实现仅按**通关地区数**给一个爆率倍率。现与生产同源：`p = Σ 权重 × min(值/参考值,1)`（权重合计 = 1），`luck = luckMax × p` 后喂给 `loot.rarity_weights` 的 `base[i]×(1+luck×i)`。**六项来源** = 通关地区数 / 装备「品阶幸运」词条 / 抽箱品阶概率料理秘药 / **远征通关** / **高难通关** / 彩蛋英雄被动（远征与高难按难度加权、仅计首通）。各参考值为真实最大值（含太古），**只有全部来源拉满才到上限**。见 `shared/data/chests.json:rarityLuck`、`backend/app/services/drop_luck.py:chest_rarity_luck`、`luck_sources.py`；回归保护 `tests/test_luck_sources.py`、`tests/test_shared_data.py:test_rarity_luck_sources_are_consistent` |
| 生产练级节奏 | PRD 未定义生产等级的经验曲线。原曲线 `growth 1.07`（1→100 总经验约 **92.6 万**）叠加偏低的配方经验，练满需 **40h+**（且最优解是反复刷低级半成品）。现按「生产 1→100 约 10h」重标定：曲线降为 `growth 1.045`（总经验约 13.7 万），配方经验整体上调约 2 倍，并新增**品阶经验系数** `xpRarityMultiplier`（装备按实际品阶 1.0→3.0 加权，材料/消耗品恒 1.0）。见 `shared/data/dohdol-levels.json:expCurve`、`scripts/gen-dohdol-data.py`、`backend/app/services/production.py:report_produce`；回归保护 `tests/test_dohdol.py:TestCraftXp` / `test_level_curve_fits_pacing_budget` |
| 专用装备词条 | PRD 只给战斗装备定义了 Buff/Debuff（2.4）。本项目让**生产/采集专用装备**也带词条（`dohdol-equipment.json:terms`，仅限专用栏位），品质规则复用同一套（普通/稀有/太古），含 Buff「品阶幸运 / 品质洞察 / 巧手 / 灵感(制造经验) / 丰收 / 渔王的直觉 / 博识(采集经验)」与 Debuff「品阶失衡 / 品质钝化 / 歉收」等；扩展后另含**产出类**词条（材料节省 / 额外产出 / 品质跃升 / 额外采集 / 珍稀 / 满载 / 双钩 及其对应 Debuff），在 `production.py` / `gathering.py` / `fishing.py` 结算时生效。词条池独立于 `terms.json`，与战斗词条同入词条图鉴（`source=production` + 类别筛选），计入 `equipped_bonus`。词条数上限为 4，故 DoH Buff 由 3 个扩到 4 个，避免神装必然带 1 个 Debuff。回归保护 `tests/test_shared_data.py:test_dedicated_terms_are_scoped_and_valid`、`tests/test_dohdol.py:TestDedicatedTerms / TestExtendedDedicatedTerms` |
| 制造出售防刷 | 制造品阶概率上限提高后，制造→出售仍是**净亏或在 t=0 时基本打平**；回归测试断言满进度下制造出售的**每制造秒金币**仍低于终局打怪金币下限（地区 40 普通怪 × 浮动下限 ÷ 8s）。见 `tests/test_dohdol.py:TestCraftEconomy` |
| 采集/渔获/半成品出售价 | 原价按档位缓慢递增（6/10/14/20/28/38），而战斗金币按篇翻倍，末期非战斗收入只有战斗的约 **0.1%**，收集材料几乎没有回报。现改为**越高档涨幅越大**的倍数曲线（第 2 档起每档翻倍：采集 6/16/32/64/128/256，普通鱼 8/20/40/80/160/320，鱼王与鱼皇分别约为同档普通鱼的 20× / 80×），半成品由固定 25 上调到 50，通用材料（3）与第 1 档保持不变。上限锚定「**低于同档战斗金币下限的 30%**」（战斗下限 = 普通怪金币 × 浮动下限 ÷ 8s），既恢复收集动机，又不产生「不打怪只采集卖钱」的路线。地区 1 为既有例外（起始武器约 11s/杀，战斗本身低于采集），不纳入断言。见 `scripts/gen-dohdol-data.py` 顶部档位表；回归保护 `tests/test_dohdol.py:TestDohdolSellBalance` |
| 副本 | 见下方「副本固定难度」「副本每日奖励上限」等条目；门槛见 `balance.json`，遥测规则见「战力平衡 v2」 |
| 副属性品质 | PRD 只给 Buff/Debuff 定义了稀有/太古（附魔 5.3）；本项目把同一套品质规则延伸到**副属性**：普通在区间内随机，稀有取上限，太古取上限 ×1.25（如暴击 100-400 → 太古 500），以 🌟 标记 |
| 英雄升级与追赶 | 英雄 60–99 级的升级经验在原曲线上逐步降低 10%–30%，1–59 级不变。低于账号当前最高英雄等级时，野外 / BOSS / 副本战斗经验在现有加成后额外 +100%；追平后的经验不再翻倍。联机奖励按真实参战英雄分别计算，克隆不领取。已有经验保留，下一次获得经验时按新曲线升级。 |
| 经验词条 | PRD 的金币 Buff（地区 5.3）只有金币类；本项目新增「经验获取效率」`expGainPct`（3%-18%，项链/耳环/手镯/戒指），由**服务端结算时**乘算，客户端只上报基础经验，避免客户端篡改经验 |
| 品阶幸运词条 | 新增**战斗**词条「品阶幸运」`chestRarityPct`（+2%~10%，太古 ×1.25），出现在全部 11 个战斗栏位，用于提升抽箱品阶概率；与原「幸运」`goldLuckPct`（金币浮动下限）互不影响，**不参与战力计算**（`economy.power.weights` 不含该 stat）。见 `shared/data/terms.json`、`backend/app/services/drop_luck.py` |
| 难度加权通关积分 | 远征（12 本）与高难（6 本）的**首通**数量按难度加权后计入品阶概率：难度表见 `multiplayer.json:difficultyWeights`（普通1/极2/零式3/绝5）与 `raids.json:difficultyWeights`（极1/绝3）；`ref` = 全部副本权重之和，故只有**全通**才把该项拉满。仅计首通（重复刷不叠加），避免挂机刷本无限抬概率。见 `backend/app/services/luck_sources.py` |
| 副本攻击标定 | 副本 BOSS 的 `hpMultiplier` / `attackMultiplier` 与双 BOSS 狂暴减伤不靠手算：用真实客户端模拟器（`BattleSimulator`）扫描，「达标装通关后剩余生命 ≈ 40%-60%」定档，技能池（6s 共享 CD）计入承伤。见 `shared/data/raids.json`；回归保护见 `tests/test_engine.py:TestRaidPressure` |
| 副本门槛 | 采用递减战力公式重标定：普通推荐 10k/25k/42k/52k，高难最低 60k/62k，另有装备与双属性门槛。 |
| 副本固定难度 | 取消「按战力动态放大 BOSS 血量/攻击」：BOSS 属性改为**固定锚定副本 `challengeLevel`（目标等级，进入等级 +5~10）**，不再随玩家等级或战力变化。低于目标等级的英雄套用与地区一致的等级压制（命中 −4%/级、输出 −8%/级、承伤 +25%/级、防御 −12%/级），因此「只到进入等级」不足以通关，需要练到目标等级并配好装备（例：目标 Lv.67 的副本 65-70 级才打得动）。见 `shared/data/raids.json:challengeLevel`、`backend/app/services/raid_util.py:raid_penalty`，回归保护见 `tests/test_engine.py:TestRaidFixedDifficulty` |
| 副本每日奖励上限 | 每个副本每天有 `dailyRewardClears` 次奖励通关（首通计入；极* 3 次、绝* 1 次），超出后仍记录通关与最快用时，但不再产出金币/经验/宝箱——配合「难度高、奖励高」的定位，防止反复刷取。计数持久化在 `raid_progress.reward_day` / `rewarded_today`（迁移 `l9d1f3a5c7e9`） |
| 副本经验加成 | 副本结算与地区同源：装备词条 + 药水/食物 + 彩蛋被动的 `expGainPct`（以及金币加成）一并计入；此前副本只乘装备词条，漏掉了药食与彩蛋被动。见 `backend/app/api/v1/raid.py:report_session` |
| 副本 BOSS 技能 | PRD 只给关底 BOSS 定义了 1 个特殊技能。本项目给「高难副本」页**每个 BOSS** 配 13 个共享技能，并改为**单一共享 CD + 随机抽取**（原为每技能独立 CD），显著提高副本压力；地区战斗仍不触发 BOSS 技能。见 `shared/data/raids.json:bossSkillPool`、`frontend/src/game/core/battle.ts` |
| 机制试炼 | 本项目曾新增「机制试炼」（服务端随机下发顺劈/冲击/蓄力提示，连续三次限时响应）作为地区与高难的资格门槛；现按需求**取消**：地区只需满足战力/装备质量/主攻/双防 + 前一地区 BOSS 通关，高难只需进入门槛 + 输出/防御/时长校验。`/trial` 接口、`mechanism_trials` 表与前端试炼组件一并移除 |
| 越级惩罚 | PRD 地区 7.3 只说「效率极低」；本项目把软惩罚做成接近硬约束：每落后 1 级 **命中 −4% / 输出 −8% / 受伤 +25% / 防御 −12%**（上限 80 / 95 / +1200% / −100%）。落后 **10+ 级几乎必败、20+ 级即使满配也几乎必败**。关键修正：受伤加成改为**减防之后乘算**，并让**防御随落后级数衰减**——否则高防御会把惩罚完全吃掉（实测落后 10 级仍能磨过去）。见 `shared/data/regions.json:levelPenalty` |
| 蓝量经济 | PRD 7.2 只写「智力影响上限与回复」。曾一度把蓝池与回复**刻意收紧**（Lv100 物理系满蓝约 600、回复仅 0.82/s），结果放几个技能就见底、之后退化成「普攻 + 最低威力技能」的循环。现改为**可持续**：`maxMp = 220 + 智力×0.4 + 等级成长`（Lv100 物理约 **1485**、魔法约 **2270**）、`mpRegen = 11 + 智力×0.01 + 等级成长`（Lv100 约 **14/s**）——**回复足以覆盖持续轮转**（Lv100 实测耗蓝 ~12.8/s），蓝条不再被清空。另外**零耗蓝普攻会回复 3% 最大蓝量**，作为见底时的兜底回蓝手段（`mp.basicAttackRestorePct`）。黑魔的「冰封回蓝 / 核爆耗蓝」随之成立。见 `shared/data/heroes.json:attributes` / `:mp`，回归保护见 `frontend/src/game/core.spec.ts:蓝量经济` |
| 治疗数值 | PRD 五.4 要求「治疗自愈能力高出 DPS 职业 50-80%」，但原治疗量（治疗 15%、医济 25%+3%×5s、天赐祝福满血、护盾 20%）配合 15s CD 可持续续命。统一**数值砍半 + CD 延长**：治疗 8%/20s、医济 12%+1.5%×5s/40s、天赐祝福由满血改为 50%/180s、护盾 10-12%/40s。见 `shared/data/jobs.json` |
| 职业技能属性 | 力量/敏捷职业的部分技能被写成「魔法伤害」，而它们的 `magicAttack` 只有物理攻击的一半左右 → 实测副本 DPS 差 **5 倍**（骑士 30s vs 龙骑 6s）。改为**所有非智力职业的技能一律物理伤害**，并把威力/秒收敛到离群职业（黑魔 90→69、龙骑 76→72）。见 `shared/data/jobs.json` |
| 管理员账号 | PRD 无此系统。账号名与密码来自环境变量（不落配置、不进镜像），启动时创建/同步；不创建英雄故天然不上榜，`refresh_all_rankings` 另做显式跳过；改密接口用**用户 ID** 定位目标（不暴露账号列表给普通用户），并禁止管理员改自己的密码 |
| 封号 | PRD 无此系统。管理员可封禁 / 解封账号（`users.banned` / `banned_at`）。封禁在服务端**每个已认证请求**上强制生效（`core/deps.get_current_user`），因此已登录的令牌在下一次请求即被拒（强制下线），且登录接口也拒绝发放令牌；**不参与排行榜**（刷新期与查询期双重过滤）。响应只回传机器码 `{"code":"banned"}`，不含任何可读文案，前端命中后**只渲染空白页**、不显示任何提示，避免被封用户反推封禁原因。不能封禁管理员。见 `core/security.BANNED_DETAIL`、`api/v1/admin.py` |
| XSS 防护 | 全部用户文本（昵称 / 英雄名 / 排行榜名）一律走 Vue 插值（自动转义），前端**没有任何 `v-html` / `innerHTML`**；昵称在入库前额外剥离尖括号与控制字符（`api/v1/auth.sanitize_nickname`），作为纵深防御。所有查询走 SQLAlchemy 绑定参数，无拼接 |
| 反多开 / 反刷 | PRD 无此系统。金币与奖励**全部由服务端结算**：兑换码请求体只有 `code`，数额取自 `REDEEM_GOLD`（客户端改前端数字无效）；战斗金币按服务端时钟与理论击杀速率封顶。在此基础上按**客户端 IP**做滑动窗口限流（`services/ratelimit.py` + `security_events` 表，落库而非内存，多 worker/重启仍准确）：注册 5/时、20/天，登录 30/5 分钟，兑换 10/时，且**同一 IP 对同一兑换码最多 5 个账号可兑换**（`redeem_records.ip`）。阈值均可通过环境变量调整；`<= 0` 关闭该项 |
| 防游戏加速 | PRD 未涉及。**服务端不采信任何客户端时间**：地区战斗的上报窗口曾取 `max(客户端 elapsedMs, 服务端间隔)`，改系统时间或用「视频加速」类插件拉快动画帧即可谎报 20s 窗口、凭空放大击杀额度；现改为**只取服务端记录的真实间隔**（`api/v1/battle.py` 的 `window_ms`），BOSS 通关耗时也以该窗口封顶（防刷榜）。副本侧早有同类校验：`clear_failures` 要求**服务端真实耗时** ≥ `minimumFightMs`，加速无效。本地模拟另有前端兜底：模拟时间按 `Date.now()` 真实墙钟发预算，篡改 `requestAnimationFrame` 时间戳加不了速。回归保护见 `tests/test_api.py:test_client_elapsed_cannot_buy_window` |

---

## 已知限制

- 第三方登录未实现（PRD 标注「开发阶段确定」）。
- 排行榜防作弊仅做速率与区间校验，未做离线行为分析与人工审核后台。
- 图鉴完成度奖励（称号/头像框/徽章）目前只展示进度，未实现奖励发放。
- 生产·采集 DLC 的材料 / 配方 / 鱼类 / 消耗品内容为首版（每地区 2 件专属材料共 80 件、4 种普通鱼 + 鱼王 + 鱼皇），
  由 `scripts/gen-dohdol-data.py` 生成、结构与数值集中可调；鱼类 / 材料图鉴已实现（见「图鉴」小节）。
- 数值平衡为初版，虽有回归测试保护节奏区间，仍建议按 PRD 做一轮平衡性测试。
- 容器编排为单机 Compose，未包含 HTTPS 证书与多实例横向扩展。


## 联机 DLC：八英雄远征

新增最多8名独立英雄、异步PvP、个人/离线/在线合作及12个团队副本。入口为「名册」「远征」「竞技场」。玩法、数值、接口与验收详见 [DLC设计文档](docs/multiplayer-dlc-design.md)。

通关记录与「远征榜」：团队副本通关瞬间由 `coop_worker` 把该次时长与全席位分角色战斗信息写入 `coop_records`（每个真实参战账号一行，外部克隆只出现在阵容展示里）。排行榜页的「远征榜」按副本实时聚合，展示各玩家最快通关时长与阵容，点击可查看分角色输出图表。

升级已有数据库前先备份并停止旧服务。PostgreSQL运行 `alembic upgrade head`；已有本地SQLite运行 `python -m app.migrate_local`（不能只依赖自动建表）。团队战斗需要独立 `python -m app.coop_worker`；Docker Compose和本地开发脚本已接入。客户端连接丢失15秒后英雄转为克隆体，重连恢复；所有操作者离线60秒终止战斗。

生产配方、本次生产和药食库存直接展示秘药/料理的全部加成及持续时间。普通战斗、地区 BOSS 和副本通关的经验日志采用服务端结算值，括号显示相对结算基础的实际增幅、效率加成和追赶加成；结算基础已包含怪物倍率与奖励效率修正。技能直接治疗及持续治疗记录实际恢复生命与溢出治疗量。采集页与生产页的日志按每次结算记录获取数量与经验，经验行逐条列出加成来源。

战斗日志与伤害浮动数字中，暴击 / 直击 / 双触发分别以 `!` / `!` / `!!` 标记，并用颜色区分（暴击橙、直击青、双触发金）；`LogEntry.tone` 相应新增 `crit` / `dh` / `critDh`（见 `frontend/src/game/core/battle.ts`、`MainView.vue`、`RaidView.vue`）。
