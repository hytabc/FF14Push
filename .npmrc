# npm 国内镜像源（npmmirror，原淘宝源）。
# npm 会把 package-lock.json 中 registry.npmjs.org 的下载地址自动替换为本源。
# 如需改回官方源或换其它镜像，改这一行即可；置空/删除本文件即恢复官方源。
registry=https://registry.npmmirror.com

# sharp 的 libvips 预编译包默认从 GitHub Releases 下载（不走 npm 源），国内网络下会长时间卡住。
# @capacitor/assets 依赖 sharp（生成 App 图标 / 打 APK、iOS 包时用到），故改走 npmmirror 二进制镜像。
# 键名对应 sharp/install/libvips.js 读取的 npm_config_sharp_libvips_binary_host。
sharp-libvips-binary-host=https://registry.npmmirror.com/-/binary/sharp-libvips
